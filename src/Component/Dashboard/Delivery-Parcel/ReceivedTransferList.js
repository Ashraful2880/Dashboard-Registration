import React from 'react';

const ReceivedTransferList = () => {
    return (
        <div>
            This is ReceivedTransferList page
        </div>
    );
};

export default ReceivedTransferList;